{
  "name": "spike-adapters",
  "private": true,
  "type": "module",
  "description": "Spike 1.6: the transports that passed, wrapped as automerge-repo network adapters",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@automerge/automerge": "^3.4.1",
    "@automerge/automerge-repo": "^2.5.6",
    "@fails-components/webtransport": "^1.4.0",
    "@fails-components/webtransport-transport-http3-quiche": "^1.4.0",
    "werift": "^0.24.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "vitest": "^5.0.0"
  }
}
